/* exported getValue */
// Define a function getValue with two parameters, object and key//
// Using dot or bracket notation, find the object at the specific key and assign to variable //
// return variable //

function getValue(object, key) {
  var prop = object[key];
  return prop;
}

// Define a function getValue with two parameters, object and key//
// using bracket notation, evaluate the object at the index key and assign to variable//
// return the variable//
