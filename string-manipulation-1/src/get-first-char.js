/* exported getFirstChar */
// Define a function with the name getFirstChar and one parameter string://
// Use the slice method of the object to extract the first character//
// return the extracted character when function is called//

function getFirstChar(string) {
  var first = string[0];
  return first;
}
// Define a function with the name getFirstChar with one parameter "string"://
// Get the value of the string at the first index and assign it to a variable//
// return that variable at the end of the function.//
