/* exported isLowerCased */
// Define a function isLowerCased with one parameter the object word//
// use the toLowerCase method of the object to make the entire word lowercase//
// and assign it to a variable//
// use an if statement to return a boolean false if //
// the object does not match the variable above with the lower case object//
// return true otherwise with an else statement//

function isLowerCased(word) {
  var lower = word.toLowerCase(word);
  if (word === lower) { return true; } else { return false; }
}
// Define a function isLowerCased with parameter, the object word//
// Use the toLowerCase method of the object to make the entire object lowercase//
// assign this value to a variable //
// Use an if statement to compare the object to the variable with the entire object lowercase//
// return the boolean true if it is indeed lower case and return boolean false if it is not.
