/* exported head */
// Define a function named head one parameter array: //
// - uses a for loop to access all values of the array//
// - returns the first value of the array at the first index//

function head(array) {
  var first = array[0];
  return first;
}
// Define a function named head with one parameter array: //
// assign the first index of the array to a variable//
// return that variable//
